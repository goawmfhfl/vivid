# 환경 설정
EXPO_PUBLIC_SUPABASE_URL=""
EXPO_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""
# WebView에 로드할 웹 앱 URL (필수)
# 시뮬레이터: http://localhost:3000
# 실제 기기(핸드폰): localhost 사용 불가. 반드시 PC IP 사용. 예) http://172.30.1.59:3000
#   - PC IP 확인: 터미널에서 ipconfig getifaddr en0 (Mac)
#   - 웹 서버는 npm run dev:host 로 실행 (apps/web)
#   - 폰과 PC 같은 Wi‑Fi 필수
EXPO_PUBLIC_WEB_APP_URL=http://localhost:3000

# 암호화 키 (64자 hex 문자열)
# 생성 방법: openssl rand -hex 32
ENCRYPTION_KEY=""

# OpenAI API 키
OPENAI_API_KEY=""

